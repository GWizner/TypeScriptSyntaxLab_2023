
for (int i = 0; i < 20; i++)
{
	for (int j = 0; j < 20; j++)
	{
		Console.Write("(.)");
	}
	Console.WriteLine();
}
//create new branch
//git checkout -b changeGridSize (shorthand to create a new branch & check it out)
//git branch to see branches
//git add
//git commit 
//git push --set-upstream origin changeGridSIze (also --set-u)
//pull request